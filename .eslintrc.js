module.exports = {
  root: true,
  extends: '@react-native-community',
  rules: {
    'no-console': 'off',
    'react-native/no-inline-styles': 0,
    // can turn off with "off"
  },
};

//  ESlint shows you the errors in your code and you can specify the rules here
// check out ESlint for rules
